<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a.com.muslimremindr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectActivity"></activity>
        <activity android:name=".AzkarMorning" />
        <activity android:name=".AzkarSalahActivity" />
        <activity android:name=".Tasbeeh3" />
        <activity android:name=".Tasbeeh2" />
        <activity android:name=".Tasbeeh" />

        <receiver android:name=".AlarmReceiver" />
        <receiver android:name=".AlarmReceiver2" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>